---
const allPosts = await Astro.glob('../pages/posts/*.md');

---


<ul>
    {
        allPosts.map(( post )=>(
            <a href={post.url}>
                <li>
                    {post.frontmatter.title}
                </li>
            </a>
        ))
    }
</ul>